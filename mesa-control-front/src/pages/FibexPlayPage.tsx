import FibexPlayFeaturePage from '../features/fibex-play/FibexPlayPage'

/** Ruta `/fibex-play`: monitoreo en vivo de la grilla de canales. */
export default function FibexPlayPage() {
  return <FibexPlayFeaturePage />
}
